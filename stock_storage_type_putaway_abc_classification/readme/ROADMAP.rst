* Split module structure in order to extract abc_storage field from
  product in a separate module as this pull the putaway strategy in parallel
  of computing the abc_storage field from classification.
