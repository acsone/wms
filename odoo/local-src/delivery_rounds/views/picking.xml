<?xml version="1.0"?>
<openerp>
  <data>

    <record id="view_picking_internal_search" model="ir.ui.view">
        <field name="name">stock.picking.tree.round</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <field name="picking_type_id" position="after">
                <field name="delivery_round_id"/>
            </field>
            <xpath expr="//filter[@name='picking_type']" position="after">
                <filter name="delivery_round" string="Delivery Round" domain="[]" context="{'group_by':'delivery_round_id'}"/>
            </xpath>
        </field>
    </record>

    <record id="view_vpicktree" model="ir.ui.view">
        <field name="name">stock.picking.tree.round</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <field name="name" position="before">
                <field name="sequence"/>
            </field>
            <field name="state" position="after">
                <field name="delivery_round_id"/>
            </field>
        </field>
    </record>


    <record id="view_vpicktree_ro" model="ir.ui.view">
        <field name="name">stock.picking.tree.round</field>
        <field name="model">stock.picking</field>
        <field name="arch" type="xml">
            <tree decoration-info="state == 'draft'" decoration-muted="state == 'cancel'" decoration-danger="state not in ('cancel', 'done') and min_date &lt; current_date" string="Picking list" create="false" delete="false" edit="false" >
                <field name="sequence"/>
                <field name="name"/>
                <field name="location_dest_id"/>
                <field name="partner_id"/>
                <field name="date" invisible="1"/>
                <field name="min_date"/>
                <field name="origin"/>
                <field name="group_id" invisible="1"/>
                <field name="backorder_id"/>
                <field name="state"/>
                <field name="delivery_round_id"/>
                <field name="priority" invisible="1"/>
                <field name="picking_type_id" invisible="1"/>
            </tree>
        </field>
    </record>


    <record id="action_picking_tree_round" model="ir.actions.act_window">
        <field name="name">Stock Operations</field>
        <field name="res_model">stock.picking</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,kanban,form,calendar</field>
        <field name="domain">[('delivery_round_state', '=', 'open')]</field>
        <field name="context">{
                'contact_display': 'partner_address',
        }
        </field>
        <field name="view_id" ref="view_vpicktree_ro"/>
        <field name="search_view_id" ref="stock.view_picking_internal_search"/>
        <field name="help" type="html">
          <p>
            List of pickings to process. Only pickings linked to confirmed delivery rounds are displayed.
          </p>
        </field>
    </record>
    <record id="action_picking_tree_available_round" model="ir.actions.act_window">
        <field name="name">Stock Operations</field>
        <field name="res_model">stock.picking</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,kanban,form,calendar</field>
        <field name="domain">[('picking_type_subcode', '=', 'PICK')]</field>
        <field name="context">{
                'search_default_delivery_round_id': active_id,
        }
        </field>
        <field name="view_id" ref="view_vpicktree_ro"/>
        <field name="search_view_id" ref="stock.view_picking_internal_search"/>
        <field name="help" type="html">
          <p>
            List of pickings attached to a delivery round.
          </p>
        </field>
    </record>

    <menuitem id="menu_round_picking" name="Rounds' Pickings" parent="menu_delivery_round" sequence="50" action="action_picking_tree_round" groups="stock.group_stock_manager,stock.group_stock_user"/>


  </data>
</openerp>
