<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_order_form" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <field name="team_id" position="after">
                <field name="sale_channel_visible" invisible="1"/>
                <field name="sale_channel" attrs="{'invisible': [('sale_channel_visible', '=', False)]}"/>
            </field>

            <field name="client_order_ref" position="replace"/>

            <field name="partner_id" position="after">
                <field name="client_order_ref"/>
            </field>

            <xpath expr="//field[@name='order_line']/tree//field[@name='product_uom_qty']" position="after">
                <field name="product_qty_unavailable"/>
            </xpath>

            <xpath expr="//field[@name='order_line']/tree//field[@name='price_subtotal']" position="after">
                <field name="exception"/>
            </xpath>

            <xpath expr="//field[@name='order_line']/tree" position="attributes">
                <attribute name="decoration-warning">exception</attribute>
            </xpath>

        </field>
    </record>

    <record id="view_order_line_available_tree" model="ir.ui.view">
        <field name="name">sale.order.line.available.tree</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <tree string="Sales Order Lines Available" create="false" edit="false" delete="false">
                <field name="product_id" invisible="1"/>
                <field name="order_id"/>
                <field name="order_partner_id"/>
                <field name="name"/>
                <field name="product_uom_qty"/>
                <field name="product_qty_unavailable" string="Quantity unavailable at order line save"/>
                <field name="current_product_qty_unavailable" string="Quantity unavailable at now"/>
                <field name="difference_qty_unavailable" string="Difference quantity unavailable"/> <!-- Remplacer quantité déjà livrée -->
            </tree>
        </field>
    </record>

    <record id="action_product_sale_available_list" model="ir.actions.act_window">
        <field name="name">Sale Order Lines Available</field>
        <field name="res_model">sale.order.line</field>
        <field name="context">{'search_default_product_id': [active_id], 'default_product_id': active_id}</field>
        <field name="domain">[('state', 'in', ['sale', 'done'])]</field>
    </record>

</odoo>
