<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_picking_withgrn_search" model="ir.ui.view">
            <field name="name">stock.picking.grn.search</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_internal_search"/>
            <field name="arch" type="xml">
                <filter name="late" position="after">
                    <filter name="grn" string="Received" domain="[('grn_id', '!=', False)]"/>
                    <filter name="nogrn" string="Not Received" domain="[('grn_id', '=', False)]"/>
                    <filter name="time_exceeded" string="Outdated receipt" domain="[('is_time_exceeded', '=', True)]"/>
                </filter>
            </field>
        </record>
        <record id="view_picking_withgrn_tree" model="ir.ui.view">
            <field name="name">stock.picking.grn.tree</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.vpicktree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="attributes">
                    <attribute name="colors">red:is_time_exceeded == True</attribute>
                </xpath>
                <field name="state" position="after">
                    <field name="grn_id" widget="boolean" string="GRN"/>
                    <field name="grn_date" widget="date"/>
                    <field name="time_delay"/>
                    <field name="is_time_exceeded" invisible="1"/>
                </field>
            </field>
        </record>
        <record id="view_picking_withgrn_form" model="ir.ui.view">
            <field name="name">stock.picking.grn.form</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="after">
                    <field name="picking_type_code" invisible="1" />
                    <field name="grn_id" attrs="{'invisible':[('picking_type_code','!=','incoming')]}" options="{'no_create':True}"/>
                    <field name="grn_date" attrs="{'invisible':[('picking_type_code','!=','incoming')]}"/>
                    <field name="time_delay" attrs="{'invisible':[('picking_type_code','!=','incoming')]}"/>
                </field>
            </field>
        </record>
</odoo>
