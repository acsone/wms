<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2023 ACSONE SA/NV
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <field name="type" position="after">
                <label for="type_delivery" string="Is also delivery" />
                <field
          name="type_delivery"
          attrs="{'invisible': [('type', '!=', 'invoice')]}"
        />
            </field>
            <xpath
        expr="//page[@name='contact_addresses']//form//field[@name='type']"
        position="after"
      >
                <field
          name="type_delivery"
          attrs="{'invisible': [('type', '!=', 'invoice')]}"
        />
                <label
          for="type_delivery"
          attrs="{'invisible': [('type', '!=', 'invoice')]}"
        />
            </xpath>
            <xpath
        expr="//field[@name='child_ids']/form//field[@name='type']"
        position="after"
      >
                <field
          name="type_delivery"
          attrs="{'invisible': [('type', '!=', 'invoice')]}"
        />
                <label
          for="type_delivery"
          attrs="{'invisible': [('type', '!=', 'invoice')]}"
        />
            </xpath>
            <xpath expr="//field[@name='type']" position="attributes">
                <attribute name="attrs" eval="{}" />
                <attribute name="groups" />
            </xpath>
            <xpath expr="//field[@name='title']" position="attributes">
                <attribute name="attrs" />
            </xpath>
            <field name="ref" position="attributes">
                <attribute name="attrs">{'readonly': True}</attribute>
            </field>
        </field>
    </record>

    <record id="view_res_partner_specific_tree" model="ir.ui.view">
        <field name="name">res.partner.specific.tree</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree" />
        <field name="arch" type="xml">
            <field name="display_name" position="after">
                <field name="type_name" />
                <field name="city" />
            </field>
        </field>
    </record>

    <record id="view_res_partner_specific_filter" model="ir.ui.view">
        <field name="name">res.partner.specific.select</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter" />
        <field name="arch" type="xml">
            <field name="category_id" position="before">
                <field name="city" />
            </field>
        </field>
    </record>

</odoo>
