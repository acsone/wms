<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">

            <div name="button_box" position="inside">
                <field name="sale_id" invisible="True"/>
                <field name="purchase_id" invisible="True"/>
                <button class="oe_stat_button" name="action_view_helpdesk_tickets"
                        type="object" icon="fa-life-ring"
                        context="{'default_ref': 'stock.picking,' + str(active_id), 'default_partner_id': partner_id, 'default_sale_order_id': sale_id, 'default_purchase_order_id': purchase_id}">
                    <field string="Tickets" name="helpdesk_tickets_count" widget="statinfo" />
                </button>
            </div>

        </field>
    </record>

    <record id="view_picking_tree" model="ir.ui.view">
        <field name="name">stock.picking.tree.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">

            <xpath expr="//tree//field[@name='state']" position="before">
              <field name="helpdesk_tickets_count" invisible="1"/>
              <button name="helpdesk_ticket_clicked" type="object"
                      fg_color="red:helpdesk_tickets_count>0;" icon="fa-life-ring"/>
            </xpath>

        </field>
    </record>

</odoo>
